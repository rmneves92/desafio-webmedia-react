import React from 'react';
import Logo from '../logo/logo';

export default (props) => (
	<footer className="footer flex-container flex-row">
		<Logo link="http://jobs.webedia.group/frontend" />
	</footer>
);
